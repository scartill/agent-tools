Execute the /speckit.superb.critique, but **DO NOT** scan changes of the folloing files:
- all *.log files
- the uv.lock file

Write the this report into the `./.scartill/critique-<feature-number>-<sequential-number>.md` (e.g. `critique-004-02.md`. Number the issues for further referencing.  Add the completion ([ ]) checkboxes to track progress. Do not overwrite existing reports; choose a new file name if needed.

